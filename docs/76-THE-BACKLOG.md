# 76 — The backlog: who is doing what, and what is blocking it

> **STATUS: LIVE — the work register.** Created 2026-08-30. Ethan: *"build me backlog so
> we can work seperately."*
>
> ⭐ **EVERY ITEM HAS ONE OWNER.** That is the entire point: two people cannot work in
> parallel from a list that does not say who is holding each piece.
>
> ⚠️ **THIS IS NOT A SECOND GAMEPLAN.** Build ORDER and chunk state live in
> `68-THE-GAMEPLAN.md`; screen/dialogue DESIGN lives in `75-THE-SCREEN-AS-A-STAGE.md`;
> findings live in `DEFECTS.md`. This is the *work register* — what is in flight, who has
> it, and what it is waiting on. Where it needs a fact from one of those, it **links**
> rather than restating it, because a fact in two documents is a fact that will disagree
> with itself.

## The order, corrected

Ethan's sequence, with the one correction that matters:

```
God dialogue (per god)   ← HERE
Player action pass
Controls pass
Whispers
Mod settings — worldgen half        🔴 the real gate
──────────── WORLD REGEN ────────────
Backstory creation
Mod settings — everything else
```

🔴 **Mod settings cannot be second-to-last.** It contains the worldgen settings, and those
are baked by the regen — that is D-112, where `tectonic.json` sat "done" for sixteen days
and would have generated at the wrong depth. The pass **splits**: the worldgen half is a
hard gate before the regen, the rest can go anywhere.

⚠️ **Backstory depends on the regen**, not the reverse. Village spawn needs MCA and the new
world, so it can be *designed* now and only *built* after. That is not a demotion; it is
the only place it can be tested.

---

# 🖊️ ETHAN — writing and rulings

Nothing here should be done by anyone else. Where a decision is listed, it is genuinely
open, not a formality.

## E1 · Blade's dialogue pass — IN FLIGHT

📄 **`docs/dialogue/blade.md`** · 239 whole lines, 56 tags, 128 fragments → 616 combined.

Generated by *running* `blade_voice.js`, so every line is one the game registers. When it
comes back: `python tools/dialogue_doc.py check blade` diffs it against the live pools.

⚠️ For **fragment** tags, any open must read against *any* close in the same tag. The 616
are all real combinations.

## E2 · The rulings that block my side

| # | decision | blocks |
|---|---|---|
| **E2a** | 🔴 **Where does Salvage speak?** *"the chat bar or maybe top right like a quest log?"* | her placement (held to last by his instruction anyway) |
| ~~**E2b**~~ | ✅ **RULED 2026-08-30 — SPLIT BY AUDIENCE.** Pathed hear **their own god**; pathless hear **Caebrim**. Not a compromise: your god warning you is the relationship working, and Caebrim reaching the pathless is already her job. ⭐ It also buys a real beat free — the first tide after taking a path, the announcement **changes voice**, and the reverse when a path is lost. Drama still scales with wave difficulty, independent of who speaks | — |
| **E2c** | ⚠️ Should chat go white for Blade too, or stay `§4§l`? Chat is a scrolling record where colour separates speakers | cosmetic, not blocking |

## E6 · The Caebrim pass — she is already two people

> Ethan, 2026-08-30: *"caebrim would be a better deep speaker since she's going to become
> god agnostic."*

⚠️ **She is not a new character to write — she is an existing one to CONSOLIDATE.** She
already speaks as two separate deep-speaker entries:

| id | as | for |
|---|---|---|
| `death_speaker` | *the Speaker* (grey) | Blade's champion |
| `death_shadow` | *the Shadow* (dark grey) | Forge's champion |

And the standing ruling (2026-08-23) is already that shape: *"no speaker for everyone
except mera and kayer should be caebrim. (except salvage). kayer speaks for herself and
alice speaks to mera."*

⭐ So "god agnostic" is the ruling finished rather than a new direction — **one Caebrim**
instead of one per god. What is yours to decide:

* Does she keep a **different name per god** (the Speaker / the Shadow), or become one
  named presence everyone meets?
* Does her voice **change** depending on whose champion she is meeting, or is the point
  that it does not?
* She now also announces the tide **to the pathless** (E2b). Is that the same Caebrim
  they meet in the dark, or does she read differently above ground?

⛔ **A structural caveat, mine not yours, but it constrains the answer:** those ids key
the `met`/stage state per player. Collapsing two entries into one is a DATA MIGRATION —
someone who has met the Speaker must not meet Caebrim again as a stranger, because *"the
first thing a speaker ever says is precisely the line that must not repeat."*

## E3 · Forge's writing overhaul

*"shorter, with more periods, and more akin to rambling."* Her pools are written as
measured lines. ⚠️ The writing and the sentence-by-sentence delivery have to land
together — rambling only reads as rambling when the sentences arrive separately.

## E4 · The remaining `[CLAUDE-DRAFT]` lines

**86 across 10 files.** These are my scaffolding wearing his character's voice, which is
the one thing I should never be trusted with.

```
forge_voice 22 · blade_voice 13 · salvage_voice 13 · deep_speaker 12 · art_voice 10
introductions 4 · forge_talk 2 · salvage_deals 2 · art_deal 1 · art_events 1
```

## E5 · Bickering scenes — IN FLIGHT

The `argue_*` tags already exist in `blade.md` (`argue_accuse`, `argue_answer`,
`argue_refuse`, `argue_threat`, `argue_unanswered`). ⭐ The delivery is already built:
`broadcast.js` runs the exchange and garbles whichever god is not yours.

---

# ⚙️ CLAUDE — structure and code

## C1 · The god pass, one god at a time

Each god is **one file** — `<god>_voice.js` — so Ethan can refine a god's writing while I
am not in it.

| god | placement | font | state |
|---|---|---|---|
| **Blade** | TOP_CENTER, y +40 | `veldora:blade` (Cinzel) | ✅ **done** |
| **Art** | dead centre, `wave`, never shakes | `veldora:art` (Cormorant Garamond) | ✅ **done** |
| **Wall** | scattered, **still** | `veldora:wall` (Metamorphous) | ✅ **done** |
| **Forge** | scattered + shake, fast beat | `veldora:forge` (Rye) | ✅ **done** |
| **Salvage** | ⛔ blocked on **E2a** | `veldora:salvage` (Special Elite) | ⛔ **held last** — Ethan: *"creatively she's in the worst state"* |

⚠️ **Placement is structure; the words are his.** I set where and how a god speaks and
never what it says.

## C2 · The systems in `75`, in dependency order

| # | thing | waiting on |
|---|---|---|
| ~~**C2a**~~ | ✅ **DONE** — `screen.js`. Not a simultaneous budget: the client shows ONE message at a time, FIFO, no clear and no reorder, so the risk is BACKLOG. The lower the priority, the emptier the queue must be |
| **C2b** | The whispers | ✅ unblocked — **next** |
| **C2c** | The crashout (a god announcing its own tide) | ✅ unblocked — every god but Salvage is styled |
| **C2d** | Wall's two-movement panic → flat line | C2b, C2c. ⚠️ *slow* typed text is **not reachable** from the command route (B2) |
| **C2e** | Tide announcer — your god if pathed, Caebrim if not | ✅ unblocked (E2b ruled); wants C2a |

🔑 **C2a comes first and is not optional.** Whispers, a crashout and a tide announcement
are three claimants on one screen. `announce.js` already solved exactly this for the bar —
*one surface, so something has to lose, and a warning behind an ambience line is a warning
that never came*. Building any of the three before the referee means retrofitting it into
all three.

## C3 · Player action pass

The interior surface (`voice.aside`) exists and covers whispers, the stalker and the
pathless interior. ⚠️ **Scope not yet agreed** — "player action" needs a sentence from
Ethan about what it covers before I guess at it.

## C4 · Controls pass

*"the mess of keybinds the game now has ... rekey them to something intuitive and
controllable so there isn't a million unused features."* 246 jars have each claimed
default keys.

⚠️ Client-side: keybinds live in `options.txt` and per-mod client configs and travel by the
**client pack**, not packwiz and not anything server-side.

## C5 · The reset gate

Everything here must be true before C2 generates. `python tools/reset_preflight.py` is the
gate and exits non-zero until it is — **an UNKNOWN counts as a failure**.

* 🔴 **D-112** — `tectonic.json` is `-64` in the repo and `-128` live. Land it with
  `config_sync.py --push-pending tectonic.json`. **Ethan's call**, it is a live config change.
* ⚠️ **C4 removals** — `the-knocker` and `grim-and-bleak`, in **five** places at once
  (`pack/mods`, `index.toml`, the index hash, `resolved.json`, and `modlist.json`'s
  **`pins.versions`** — the one that gets missed). A partial removal is worse than none.
* ⚠️ `simple-hats` is installed and *also* listed under `cut_for_budget`. Bookkeeping, and
  **it is not being removed** — Ethan ruled that explicitly.

---

# 🔴 BLOCKED / AT RISK — not owned by either of us yet

## B1 · The client pack has to be rebuilt and redistributed

⭐ The fonts are delivered to Ethan's instance and to `clientpack/kubejs/assets/`, and
KubeJS loads that as an always-active resource pack — nobody enables anything.

🔴 **But anyone whose client pack predates today sees vanilla text and no error.** And
`docs/08` and `docs/09` both point at `.\server\scripts\build-client.ps1`, **which does not
exist at that path**. So the documented rebuild step is already stale, and it is the step
that puts fonts on other people's screens.

⚠️ This will surface at the reset whether or not it is dealt with first.

## B2 · The typewriter speed is not reachable

Measured 2026-08-30: a character costs a **tick**, so typing works and `TYPEWRITER` is on.
But `sendcustom` hardcodes `typewriter(1.0f, false)` — **the speed itself cannot be set
from the command route**.

⚠️ `75` asks for Wall's flat line to be *"slow ... typed"*. Slow is not available. That
needs the Java API, and reflection into this mod is **dead** (D-123) — so it is a real
piece of work, not a parameter.

## B3 · Defect IDs are racing between channels

I picked D-113 and it was taken by the tide channel before I filed. **Check the highest ID
at file time, not at draft time.**

---

## What I will pick up next, unprompted

Art → Wall → Forge, then **C2a the priority model**, then whispers. Salvage stays last.
If Ethan wants a different order, this file is where to say so.
